<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieSearchApplication" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_toolbar</item>
        <item name="colorOnPrimary">@color/main_dark_status_bar</item>
        <item name="colorSecondary">@color/primary_toolbar</item>
        <item name="colorSecondaryVariant">@color/primary_toolbar</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">@color/primary_toolbar</item>

    </style>

    <style name="Theme.MovieSearchApplication" parent="Base.Theme.MovieSearchApplication" />

    <style name="MyProgress" parent="Base.Widget.AppCompat.ProgressBar">
        <item name="color">@color/white</item>
    </style>
</resources>